# Nested upstream StackRox clone — local source of truth (own git remote)
stackrox/

# Local credentials — never commit
.secrets/

# Feature prototype build output (source stays tracked)
**/dist/
**/node_modules/

.DS_Store
*.log

# Staging API dumps may contain live tokens — never commit
mid-server-risk-export.json
*-risk-export.json
.git-filter-repo-expressions.txt
